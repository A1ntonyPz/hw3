<h1>Places</h1>

<p>
  <%= link_to "Add a new place", new_place_path %>
</p>

<% if @places.any? %>
  <ul>
    <% @places.each do |place| %>
      <li>
        <%= link_to place.name, place_path(place) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No places yet.</p>
<% end %>
